# Absolute path to the SQLite database (must match Python app)
SQLITE_PATH=/absolute/path/to/suppliersync_demo/suppliersync/suppliersync.db

# Orchestrator API URL (defaults to http://localhost:8000)
ORCHESTRATOR_API_URL=http://localhost:8000

